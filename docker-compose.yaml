version: '3.8'

services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.43.1-jammy
    container_name: demoqa-playwright
    working_dir: /app
    volumes:
      - ./:/app
    command: npx playwright test
    environment:
      - HOME=/root
    shm_size: '2gb'
